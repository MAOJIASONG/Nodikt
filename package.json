{
  "name": "framework-nodkit-v0.3",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "npm run build -w server && npm run build -w web",
    "dev": "npm run dev -w server",
    "dev:web": "npm run dev -w web"
  }
}
