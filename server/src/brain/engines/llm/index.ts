/**
 * 文件名称：index.ts
 * 文件作用：LLM 引擎统一导出入口，暴露模型客户端及相关错误类型。
 *
 * 主要职责：
 * 1. 转发 client 模块中的公共导出。
 * 2. 为规划、决策等引擎提供统一导入路径。
 *
 * 依赖模块：
 * - client：模型调用客户端实现。
 *
 * 注意事项：
 * - 本文件只做导出聚合，不应加入模型调用副作用。
 */
export * from "./client.js";
