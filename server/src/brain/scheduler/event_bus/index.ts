/**
 * 文件名称：index.ts
 * 文件作用：调度事件总线统一导出入口，集中暴露事件总线实现和处理器类型。
 *
 * 主要职责：
 * 1. 转发 EventBus 实现。
 * 2. 转发事件处理器上下文和映射类型。
 *
 * 依赖模块：
 * - eventBus：事件总线实现。
 * - types：事件处理器类型定义。
 *
 * 注意事项：
 * - 本文件只做导出聚合，不应加入运行时副作用。
 */
export * from "./eventBus.js";
export * from "./types.js";
